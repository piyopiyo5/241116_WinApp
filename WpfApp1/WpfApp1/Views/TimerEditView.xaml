<Window x:Class="WpfApp1.Views.TimerEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.Views"
        mc:Ignorable="d"
        Title="TimerEditView"
        SizeToContent="WidthAndHeight">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>
        <!-- タイマーの経過時間を表示 -->
        <TextBlock Grid.Row="0" Text="{Binding CountUpTimerText}" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
        <Button Grid.Row="1" Content="+10min" Command="{Binding TimerIncrementCommand}" Width="120" Height="30" Margin="2" />
        <Button Grid.Row="2" Content="+10min" Command="{Binding TimerDincrementCommand}" Width="120" Height="30" Margin="2" />
        <Button Grid.Row="3" Content="OK" Width="120" Height="30" Margin="10" Click="SaveButton_Click"/>
    </Grid>
</Window>
